# Build artifacts — regenerated by test_cpu6502.jl's build!() (clang -O0) every
# run, so they are never committed (unlike test/reference/c/*.ll, which are
# committed fixtures because that harness does NOT rebuild).
*.O0.ll
cpu6502_golden
# Scratch de-risk probe — superseded by the full test_cpu6502.jl harness.
step1_derisk.jl
